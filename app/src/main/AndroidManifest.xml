<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moneymind"
        tools:targetApi="31">

        <activity
            android:name=".views.activities.Account_page"
            android:exported="true" />
        <activity
            android:name=".views.activities.Add_Records"
            android:exported="true" />
        <activity
            android:name=".RecyclerRecordsAdapter$Records"
            android:exported="true" />
        <activity
            android:name=".views.activities.Splashscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.Registration"
            android:exported="true" />
        <activity
            android:name=".views.activities.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".views.activities.MainActivity"
            android:exported="true" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>